import { defineConfig } from 'vite';

// Intentionally minimal — @storybook/react-vite injects the React plugin and
// its own build settings. See main.ts for why the library config is not used.
export default defineConfig({});
